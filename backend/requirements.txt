# Runtime dependencies for the active RADICI Flask application.
# This file intentionally stays focused on what the web app needs at runtime.
# Heavier data-pipeline-only dependencies live in data_pipeline/requirements.txt.

Flask==3.1.0
flask-cors==5.0.1
gunicorn==23.0.0

numpy==2.0.2
redis==5.2.1
faiss-cpu==1.10.0

nltk==3.9.1
sentence-transformers==4.1.0
